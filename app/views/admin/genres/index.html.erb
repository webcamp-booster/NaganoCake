<h4 class="m-5 bg-light">ジャンル一覧・追加</h4>

<div class ="row">
  <%= form_with model: [:admin, @genre], class: 'form-group' do |f| %>
    <div class="col-12">
      <strong>
        <%= f.label :name, "ジャンル名", class: 'form-label mr-5' %>
      </strong>
      <%= f.text_field :name, placeholder: 'ジャンル名' %>
      <%= f.submit "新規登録", class: 'btn btn-success px-4 py-2 ml-5' %>
    </div>
  <% end %>
</div>
<div class="row">
  <div class="col-4">
    <table class="table border-bottom ">
      <thead class="table bg-light">
        <tr>
          <th class="text-center">ジャンル</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @genres.each do |genre| %>
        <tr>
          <td><%= genre.name %></td>
          <td >
            <%= link_to "編集する", edit_admin_genre_path(genre), class: 'btn btn-success px-4 py-2' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
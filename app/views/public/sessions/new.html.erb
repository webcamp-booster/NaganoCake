<div class='container'>
  <div class='row d-flex justify-content-center'>
    <div class='col-10 pt-5'>
      <span class='fs-8 bg-secondary text-black'>ログイン</span>
    </div>
  </div>
  <div class='row d-flex justify-content-center'>
    <div class='col-10 mt-5'>
      <span class='fs-8 bg-secondary text-black'>会員の方はこちらからログイン</span>
    </div>
  </div>
</div>

<div class='container mt-5'>
<!--↓ form_for(resource, as: resource_name, url: session_path(resource_name)) から変更-->
<%= form_with model: @customer, url: customer_session_path do |f| %>
    <div class="row form-group d-flex justify-content-center">
      <label class='col-2'>メールアドレス</label>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control col-5' %>
    </div>
      
    <div class="row form-group d-flex justify-content-center">
      <label class='col-2'>パスワード</label>
      <%= f.password_field :password, autocomplete: "current-password",  class: 'form-control col-5' %>
    </div>
    
    <div class="form-group d-flex justify-content-center">
      <label class='col-2'></label>
      <%= f.submit "ログイン", class: 'btn btn-primary col-3' %>
    </div>
  <% end %>
</div>
  
<div class='container mb-5'>
  <div class='row d-flex justify-content-center'>
    <div class='col-10 mt-5'>
      <span class='fs-8 bg-secondary text-black'>会員登録がお済みでない方</span></br>
      <%= render "public/shared/links" %><!--publicsから単数形に変更済-->
      から新規登録を行なってください。
    </div>
  </div>
</div>
